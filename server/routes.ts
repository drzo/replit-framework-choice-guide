import type { Express } from "express";

export function registerRoutes(app: Express) {
  // Framework recommendations are handled client-side
  // Add API routes here if needed for future extensions
}
